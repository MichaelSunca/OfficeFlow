<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.officeflow.backend.mapper.UserMapper">

    <select id="selectUserByName" resultType="com.officeflow.backend.vo.UserVO">
        SELECT
            u.id,
            u.username,
            u.password,
            u.nickname,
            u.role_id,
            u.status,
            r.role_key,
            r.role_name
        FROM sys_user u
                 LEFT JOIN sys_role r ON u.role_id = r.id
        WHERE u.username = #{username}
          AND u.status = 1
          AND u.del_flag = 0
    </select>

</mapper>